<section class="post" id="post-<%= post.id %>">
  <header class="post-header">
    <h2 class="post-title">
      <a href="<%= Web::Routes.url(:posts, id: post.id) %>"><%= post.title %></a>
    </h2>
  </header>

  <div class="post-content"><%= post.content %></div>
</section>
